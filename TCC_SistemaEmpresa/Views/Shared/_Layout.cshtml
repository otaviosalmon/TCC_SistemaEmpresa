@{
    var autenticado = User.Identity?.IsAuthenticated == true;
}
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - L.O. Solutions</title>
    @* app.css primeiro: define as variáveis de cor que sidebar.css consome. *@
    <link rel="stylesheet" href="~/css/app.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/sidebar.css" asp-append-version="true" />
</head>
<body>
    <div class="app-shell">
        @* Sem menu para quem não está logado — a barra mostra empresa e usuário. *@
        @if (autenticado)
        {
            <partial name="_Sidebar" />
        }

        <main class="app-conteudo" role="main">
            @RenderBody()
        </main>
    </div>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
