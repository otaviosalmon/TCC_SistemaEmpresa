@{
    // Usa o layout padrão (sidebar): quem chega aqui está autenticado, apenas
    // sem permissão para a tela pedida — precisa conseguir navegar para outra.
    ViewData["Title"] = "Acesso negado";
}

<div class="pagina-titulo-centro">
    <h1 class="pagina-titulo">Acesso negado</h1>
</div>

<p class="mensagem-centro">Seu perfil não tem permissão para acessar esta página.</p>

<div class="form-acoes form-acoes-centro">
    <a class="btn btn-claro" asp-controller="Home" asp-action="Index">Voltar ao início</a>
</div>
